{
  "name": "@for-little-ones/spike-commerce",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spike C — commerce candidate vs minimal self-built commerce surface (cart → payment → order). Isolated, throwaway. Not part of the production workspaces.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@for-little-ones/domain": "file:../../packages/domain",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}